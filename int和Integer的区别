##int和Integer有什么区别？谈谈Integer的值缓存范围？

int就是我们所说的整形数字，是Java8个原始数据类型之一(int,float,boolean,byte,short,char,double,long)
Java虽说是一切都是对象，但是原始数据是例外。

Integer是int对应的包装类，在Java5中，引入了自动拆箱和自动装箱的特性，Java可以根据上下文，自动拆装箱，极大的
简化了相关的编程。

关于Integer的缓存，在Java5新增了静态工厂方法Valueof,在调用它的时候，会利用一个缓存机制，能带来明显地性能提升
按照Javadoc，这个值默认为-128到127。

理解自动装箱、拆箱

自动装修实际算是一种语言糖，可以理解为Java平台为我们自动进行了一些转换，保证不同的写法在运行时等价，他们发生在编译阶段，
也就是生成的字节码是一致的。

对象由三部分组成，对象头，对象实例，对齐填充
其中对象头一般是十六个字节，包括两部分，第一部分有哈希码，锁状态标志，线程持有的锁，偏向线程id等。第二部分是类型指针，也就是对象
指向它的类元数据指针，可以理解成对象指向它的类。
对象实例就是真正对象存储的有效信息
对齐填充，就是充当一个类似占位符的作用，因为内存的使用都会被填充成八字节的倍数。
